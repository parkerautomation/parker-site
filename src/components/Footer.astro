import site from '../config/site';

const currentYear = new Date().getFullYear();
const navItems = site.nav.footer;

<footer class="bg-gray-900 text-white pt-16 pb-8">
  <div class="container-custom">
    <div class="mb-8">
      <a href="/" class="flex items-center mb-6" aria-label="Go to homepage">
        <img src={site.logoPath} alt={site.name} class="h-8 w-auto" /> {/* TODO: add asset locally */}
        <span class="ml-2 text-xl font-display font-semibold">{site.name}</span>
      </a>
      <p class="text-gray-400 mb-6 max-w-md">{site.tagline}</p>
      <div class="flex space-x-4">
        <a href={site.social.x} target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:text-[var(--brand)] transition-colors" aria-label="X">
          {/* X icon */}
        </a>
        <a href={site.social.linkedin} target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:text-[var(--brand)] transition-colors" aria-label="LinkedIn">
          {/* LinkedIn icon */}
        </a>
        <a href={site.social.github} target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:text-[var(--brand)] transition-colors" aria-label="GitHub">
          {/* GitHub icon */}
        </a>
      </div>
      <div class="mt-4 space-y-1 text-gray-400 text-sm">
        {site.contact.email && (
          <p><a href={`mailto:${site.contact.email}`} class="hover:text-[var(--brand)] transition-colors">{site.contact.email}</a></p>
        )}
        {site.contact.phone && (
          <p><a href={`tel:${site.contact.phone}`} class="hover:text-[var(--brand)] transition-colors">{site.contact.phone}</a></p>
        )}
        {site.contact.address && <p>{site.contact.address}</p>}
      </div>
    </div>

    <nav class="mb-8">
      <ul class="flex flex-col md:flex-row md:space-x-6 space-y-2 md:space-y-0">
        {navItems.map(item => (
          <li>
            <a
              href={item.href}
              target={item.external ? '_blank' : undefined}
              rel={item.external ? 'noopener noreferrer' : undefined}
              aria-current={Astro.url.pathname === item.href ? 'page' : undefined}
              class="text-gray-400 hover:text-[var(--brand)] transition-colors"
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="border-t border-gray-800 pt-8 mt-8 text-center">
      <p class="text-gray-400 text-sm">&copy; {currentYear} {site.name}. All rights reserved.</p>
    </div>
  </div>
</footer>
